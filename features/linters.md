# Linters

This feature adds linting and style checking to the project.

## Implementation

Ensure that golangci-lint is available for this project. It should use the default configuration.

Add a make target `make lint` that runs golangci-lint.
